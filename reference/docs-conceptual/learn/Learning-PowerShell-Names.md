---
ms.date: 08/24/2018
keywords: powershell,cmdlet
title: Lernen von PowerShell-Befehlsnamen
ms.assetid: b4d0fd22-8298-4ee6-82ae-9b6f2907c986
ms.openlocfilehash: 8d50ca03f98ed4ca8f9c09c83ae57afbf0d7888d
ms.sourcegitcommit: e7445ba8203da304286c591ff513900ad1c244a4
ms.translationtype: HT
ms.contentlocale: de-DE
ms.lasthandoff: 04/23/2019
ms.locfileid: "62086424"
---
# <a name="learning-powershell-command-names"></a><span data-ttu-id="e0eef-103">Lernen von PowerShell-Befehlsnamen</span><span class="sxs-lookup"><span data-stu-id="e0eef-103">Learning PowerShell command names</span></span>

<span data-ttu-id="e0eef-104">Das Erlernen der Namen von Befehlen und Parametern ist bei den meisten Befehlszeilenschnittstellen mit einem hohen Zeitaufwand verbunden.</span><span class="sxs-lookup"><span data-stu-id="e0eef-104">Learning names of commands and parameters requires a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="e0eef-105">Das Problem liegt darin, dass nur wenige Muster vorhanden sind.</span><span class="sxs-lookup"><span data-stu-id="e0eef-105">The issue is that there are few patterns.</span></span> <span data-ttu-id="e0eef-106">Die einzige Möglichkeit besteht darin, die Befehle und Parameter auswendig zu lernen, die Sie regelmäßig verwenden müssen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-106">Memorization is the only way to learn the commands and parameters that you need to use on a regular basis.</span></span>

<span data-ttu-id="e0eef-107">Wenn Sie mit einem neuen Befehl oder Parameter arbeiten, können Sie nicht immer auf Bekanntes zurückgreifen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-107">When you work with a new command or parameter, you can't always use what you already know.</span></span> <span data-ttu-id="e0eef-108">Sie müssen nach neuen Namen suchen und diese lernen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-108">You have to find and learn a new name.</span></span> <span data-ttu-id="e0eef-109">Üblicherweise umfassen Befehlszeilenschnittstellen zu Beginn lediglich einen kleinen Satz an Tools und wachsen mit zunehmenden Ergänzungen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-109">Traditionally, command-line interfaces start with a small set of tools and grow with incremental additions.</span></span> <span data-ttu-id="e0eef-110">Es ist leicht zu verstehen, warum es keine Standardstruktur gibt.</span><span class="sxs-lookup"><span data-stu-id="e0eef-110">It's easy to see why there's no standard structure.</span></span>
<span data-ttu-id="e0eef-111">Dies erscheint bei Befehlsnamen logisch, da jeder Befehl ein eigenes Tool darstellt.</span><span class="sxs-lookup"><span data-stu-id="e0eef-111">This seems logical for command names since each command is a separate tool.</span></span> <span data-ttu-id="e0eef-112">PowerShell bietet eine bessere Möglichkeit, Befehlsnamen zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="e0eef-112">PowerShell has a better way to handle command names.</span></span>

## <a name="learning-command-names-in-traditional-shells"></a><span data-ttu-id="e0eef-113">Erlernen von Namen in herkömmlichen Shells</span><span class="sxs-lookup"><span data-stu-id="e0eef-113">Learning command names in traditional shells</span></span>

<span data-ttu-id="e0eef-114">Die meisten Befehle werden erstellt, um Elemente des Betriebssystems oder von Anwendungen, z. B. Dienste oder Prozesse, zu verwalten.</span><span class="sxs-lookup"><span data-stu-id="e0eef-114">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="e0eef-115">Die Befehle haben Namen, die zu einer Familie gehören oder auch nicht.</span><span class="sxs-lookup"><span data-stu-id="e0eef-115">The commands have names that may or may not fit into a family.</span></span> <span data-ttu-id="e0eef-116">Auf Windows-Systemen können Sie z.B. die Befehle `net start` und `net stop` zum Starten und Beenden eines Diensts verwenden.</span><span class="sxs-lookup"><span data-stu-id="e0eef-116">For example, on Windows systems, you can use the `net start` and `net stop` commands to start and stop a service.</span></span> <span data-ttu-id="e0eef-117">**Sc.exe** ist ein weiteres Tool zur Dienststeuerung für Windows.</span><span class="sxs-lookup"><span data-stu-id="e0eef-117">**Sc.exe** is another service control tool for Windows.</span></span> <span data-ttu-id="e0eef-118">Dieser Name passt nicht in das Namensmuster für die **net.exe**-Dienstbefehle.</span><span class="sxs-lookup"><span data-stu-id="e0eef-118">That name does not fit into the naming pattern for the **net.exe** service commands.</span></span> <span data-ttu-id="e0eef-119">Für die Verwaltung von Prozessen bietet Windows den Befehl **tasklist.exe** zum Auflisten von Prozessen und den Befehl **taskkill.exe** zum Beenden von Prozessen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-119">For process management, Windows has the **tasklist.exe** command to list processes and the **taskkill.exe** command to kill processes.</span></span>

<span data-ttu-id="e0eef-120">Darüber hinaus verwenden diese Befehle irreguläre Parameterspezifikationen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-120">Also, these commands have irregular parameter specifications.</span></span> <span data-ttu-id="e0eef-121">Sie können den Befehl `net start` nicht zum Starten eines Diensts auf einem Remotecomputer verwenden.</span><span class="sxs-lookup"><span data-stu-id="e0eef-121">You can't use the `net start` command to start a service on a remote computer.</span></span> <span data-ttu-id="e0eef-122">Mit dem Befehl **sc.exe** kann Dienst auf einem Remotecomputer gestartet werden.</span><span class="sxs-lookup"><span data-stu-id="e0eef-122">The **sc.exe** command can start a service on a remote computer.</span></span> <span data-ttu-id="e0eef-123">Um den Remotecomputer anzugeben, müssen sie seinem Namen jedoch einen doppelten umgekehrten Schrägstrich voranstellen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-123">But to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="e0eef-124">Um den Spoolerdienst auf einem Remotecomputer namens „DC01“ zu starten, geben Sie `sc.exe \\DC01 start spooler` ein.</span><span class="sxs-lookup"><span data-stu-id="e0eef-124">To start the spooler service on a remote computer named DC01, you type `sc.exe \\DC01 start spooler`.</span></span>
<span data-ttu-id="e0eef-125">Um die auf „DC01“ ausgeführten Aufgaben aufzulisten, verwenden Sie den Parameter **/S** und den Computernamen ohne Schrägstriche.</span><span class="sxs-lookup"><span data-stu-id="e0eef-125">To list tasks running on DC01, you use the **/S** parameter and the computer name without backslashes.</span></span> <span data-ttu-id="e0eef-126">Beispiel: `tasklist /S DC01`.</span><span class="sxs-lookup"><span data-stu-id="e0eef-126">For example, `tasklist /S DC01`.</span></span>

> [!NOTE]
> <span data-ttu-id="e0eef-127">Vor PowerShell v6 war `sc` ein Alias für das Cmdlet `Set-Content`.</span><span class="sxs-lookup"><span data-stu-id="e0eef-127">Prior to PowerShell v6, `sc` was an alias for the `Set-Content` cmdlet.</span></span> <span data-ttu-id="e0eef-128">Aus diesem Grund müssen Sie zum Ausführen des **sc.exe**-Befehls in einer früheren PowerShell-Version als v6 den vollständigen Dateinamen **sc.exe** einschließlich der Dateierweiterung **exe** einbeziehen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-128">Therefore, to run the **sc.exe** command in a version of PowerShell prior to v6, you must include the full filename **sc.exe** including the file extension **exe**.</span></span>

<span data-ttu-id="e0eef-129">Dienste und Prozesse sind Beispiele für verwaltbare Elemente auf einem Computer mit gut definierten Lebenszyklen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-129">Services and processes are examples of manageable elements on a computer that have well-defined life cycles.</span></span> <span data-ttu-id="e0eef-130">Sie können einen Dienst oder Prozess starten oder beenden oder eine Liste aller derzeit ausgeführten Dienste und Prozesse abrufen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-130">You may start or stop services and processes, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="e0eef-131">Wenngleich es wichtige technische Unterschiede zwischen ihnen gibt, sind die für Dienste und Prozesse ausgeführten Aktionen vom Konzept her identisch.</span><span class="sxs-lookup"><span data-stu-id="e0eef-131">Although there are important technical distinctions between them, the actions you perform on services and processes are conceptually the same.</span></span> <span data-ttu-id="e0eef-132">Darüber hinaus können auch Entscheidungen, die wir zum Anpassen einer Aktion durch das Angeben von Parametern treffen, konzeptionell vergleichbar sein.</span><span class="sxs-lookup"><span data-stu-id="e0eef-132">Furthermore, the choices we make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="e0eef-133">PowerShell nutzt diese Ähnlichkeiten zum Verringern der Anzahl unterschiedlicher Namen, die Sie kennen müssen, um Cmdlets zu verstehen und zu nutzen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-133">PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

## <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="e0eef-134">Cmdlets kombinieren für eine bessere Einprägsamkeit Verben und Nomen in ihrem Namen</span><span class="sxs-lookup"><span data-stu-id="e0eef-134">Cmdlets use verb-noun names to reduce command memorization</span></span>

<span data-ttu-id="e0eef-135">PowerShell verwendet eine Verb-Nomen-Namenskonvention.</span><span class="sxs-lookup"><span data-stu-id="e0eef-135">PowerShell uses a "verb-noun" naming system.</span></span> <span data-ttu-id="e0eef-136">Jeder Cmdlet-Name besteht aus einem Standardverb, an das ein Bindestrich und ein bestimmtes Nomen angehängt wird.</span><span class="sxs-lookup"><span data-stu-id="e0eef-136">Each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="e0eef-137">PowerShell-Verben sind nicht immer englische Verben, drücken aber bestimmte Aktionen in PowerShell aus.</span><span class="sxs-lookup"><span data-stu-id="e0eef-137">PowerShell verbs are not always English verbs, but they express specific actions in PowerShell.</span></span> <span data-ttu-id="e0eef-138">Die Nomen ähneln denen in einer beliebigen Sprache.</span><span class="sxs-lookup"><span data-stu-id="e0eef-138">Nouns are very much like nouns in any language.</span></span> <span data-ttu-id="e0eef-139">Sie beschreiben bestimmte Arten von Objekten, die bei der Systemadministration wichtig sind.</span><span class="sxs-lookup"><span data-stu-id="e0eef-139">They describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="e0eef-140">Anhand einiger Beispiele ist einfach zu veranschaulichen, wie diese zweiteiligen Namen den Lernaufwand verringern.</span><span class="sxs-lookup"><span data-stu-id="e0eef-140">It's easy to demonstrate how these two-part names reduce learning effort by looking at a few examples.</span></span>

<span data-ttu-id="e0eef-141">PowerShell umfasst einen empfohlenen Satz an Standardverben.</span><span class="sxs-lookup"><span data-stu-id="e0eef-141">PowerShell has a recommended set of standard verbs.</span></span> <span data-ttu-id="e0eef-142">Nomen sind weniger eingeschränkt, beschreiben aber stets, worauf das Verb angewendet wird.</span><span class="sxs-lookup"><span data-stu-id="e0eef-142">Nouns are less restricted, but always describe what the verb acts upon.</span></span> <span data-ttu-id="e0eef-143">PowerShell umfasst Befehle wie z.B. `Get-Process`, `Stop-Process`, `Get-Service` und `Stop-Service`.</span><span class="sxs-lookup"><span data-stu-id="e0eef-143">PowerShell has commands such as `Get-Process`, `Stop-Process`, `Get-Service`, and `Stop-Service`.</span></span>

<span data-ttu-id="e0eef-144">In diesem Fall von zwei Nomen und zwei Verben vereinfacht die Konsistenz den Lernprozess nicht wesentlich.</span><span class="sxs-lookup"><span data-stu-id="e0eef-144">For this example of two nouns and verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="e0eef-145">Erweitern Sie diese Liste auf einen standardisierten Satz von 10 Verben und 10 Nomen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-145">Extend that list to a standardized set of 10 verbs and 10 nouns.</span></span> <span data-ttu-id="e0eef-146">Jetzt müssen Sie nur 20 Wörter kennen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-146">Now you only have 20 words to understand.</span></span>
<span data-ttu-id="e0eef-147">Aber diese Wörter können zu insgesamt 100 unterschiedlichen Befehlsnamen kombiniert werden.</span><span class="sxs-lookup"><span data-stu-id="e0eef-147">But those words can be combined to form 100 distinct command names.</span></span>

<span data-ttu-id="e0eef-148">Die Wirkung eines PowerShell-Befehls kann leicht an seinem Namen abgelesen werden.</span><span class="sxs-lookup"><span data-stu-id="e0eef-148">It's easy to understand what a PowerShell command does by reading its name.</span></span> <span data-ttu-id="e0eef-149">Der Befehl zum Herunterfahren eines Computers lautet `Stop-Computer`.</span><span class="sxs-lookup"><span data-stu-id="e0eef-149">The command to shut down a computer is `Stop-Computer`.</span></span> <span data-ttu-id="e0eef-150">Der Befehl zum Auflisten aller Computer im Netzwerk lautet `Get-Computer`.</span><span class="sxs-lookup"><span data-stu-id="e0eef-150">The command to list all computers on a network is `Get-Computer`.</span></span> <span data-ttu-id="e0eef-151">Der Befehl um Abrufen der Systemdaten lautet `Get-Date`.</span><span class="sxs-lookup"><span data-stu-id="e0eef-151">The command to get the system date is `Get-Date`.</span></span>

<span data-ttu-id="e0eef-152">Über den **Verb**-Parameter für `Get-Command` können Sie alle Befehle mit einem bestimmten Verb auflisten.</span><span class="sxs-lookup"><span data-stu-id="e0eef-152">You can list all commands that include a particular verb with the **Verb** parameter for `Get-Command`.</span></span> <span data-ttu-id="e0eef-153">Um beispielsweise alle Cmdlets anzuzeigen, die das Verb `Get` verwenden, geben Sie Folgendes ein:</span><span class="sxs-lookup"><span data-stu-id="e0eef-153">For example, to see all cmdlets that use the verb `Get`, type:</span></span>

```
PS> Get-Command -Verb Get

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="e0eef-154">Verwenden Sie den **Noun**-Parameter, um eine Familie von Befehlen anzuzeigen, die sich auf den gleichen Objekttyp auswirken.</span><span class="sxs-lookup"><span data-stu-id="e0eef-154">Use the **Noun** parameter to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="e0eef-155">Führen Sie beispielsweise den folgenden Befehl aus, um die verfügbaren Befehle zum Verwalten von Diensten anzuzeigen:</span><span class="sxs-lookup"><span data-stu-id="e0eef-155">For example, run following command to see the commands  available for managing services:</span></span>

```
PS> Get-Command -Noun Service

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str...
...
```

## <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="e0eef-156">Cmdlets verwenden Standardparameter</span><span class="sxs-lookup"><span data-stu-id="e0eef-156">Cmdlets use standard parameters</span></span>

<span data-ttu-id="e0eef-157">Wie bereits erwähnt, weisen in herkömmlichen Befehlsschnittstellen verwendete Befehle nicht immer konsistente Parameternamen auf.</span><span class="sxs-lookup"><span data-stu-id="e0eef-157">As noted earlier, commands used in traditional command-line interfaces don't always have consistent parameter names.</span></span> <span data-ttu-id="e0eef-158">Parameter sind oft einzelne Zeichen oder abgekürzte Wörter, die schnell eingegeben werden können, aber für neue Benutzer nicht leicht verständlich sind.</span><span class="sxs-lookup"><span data-stu-id="e0eef-158">Parameters are often single-character or abbreviated words that are easy to type but aren't easily understood by new users.</span></span>

<span data-ttu-id="e0eef-159">Im Gegensatz zu den meisten anderen herkömmlichen Befehlszeilenschnittstellen verarbeitet PowerShell Parameter direkt und nutzt diesen direkten Zugriff auf die Parameter (zusammen mit Anleitungen für Entwickler), um Parameternamen zu standardisieren.</span><span class="sxs-lookup"><span data-stu-id="e0eef-159">Unlike most other traditional command-line interfaces, PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="e0eef-160">Diese Anleitung empfiehlt, garantiert aber nicht, dass jedes Cmdlet dem Standard entspricht.</span><span class="sxs-lookup"><span data-stu-id="e0eef-160">This guidance encourages but does not guarantee that every cmdlet conforms to the standard.</span></span>

<span data-ttu-id="e0eef-161">PowerShell standardisiert außerdem das Trennzeichen für Parameter.</span><span class="sxs-lookup"><span data-stu-id="e0eef-161">PowerShell also standardizes the parameter separator.</span></span> <span data-ttu-id="e0eef-162">Parameternamen verwenden immer einen angehängten Bindestrich („-“), auf den ein PowerShell-Befehl folgt.</span><span class="sxs-lookup"><span data-stu-id="e0eef-162">Parameter names always have a '-' prepended to them with a PowerShell command.</span></span> <span data-ttu-id="e0eef-163">Betrachten Sie das folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="e0eef-163">Consider the following example:</span></span>

```powershell
Get-Command -Name Clear-Host
```

<span data-ttu-id="e0eef-164">Der Parametername lautet **Name**, wird aber eingegeben als `-Name`, wenn er in der Befehlszeile als Parameter verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="e0eef-164">The parameter's name is **Name**, but it is typed as `-Name` when used on the command line as a parameter.</span></span>

<span data-ttu-id="e0eef-165">Es folgen einige allgemeine Merkmale der Standardparameternamen und ihre Verwendungen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-165">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

### <a name="the-help-parameter-"></a><span data-ttu-id="e0eef-166">Der Parameter „Hilfe“ (?)</span><span class="sxs-lookup"><span data-stu-id="e0eef-166">The Help parameter (?)</span></span>

<span data-ttu-id="e0eef-167">Wenn Sie den Parameter `-?` für ein beliebiges Cmdlet angeben, zeigt PowerShell Hilfeinformationen für das Cmdlet an.</span><span class="sxs-lookup"><span data-stu-id="e0eef-167">When you specify the `-?` parameter on any cmdlet, PowerShell displays help for the cmdlet.</span></span>
<span data-ttu-id="e0eef-168">Das Cmdlet wird nicht ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="e0eef-168">The cmdlet is not executed.</span></span>

### <a name="common-parameters"></a><span data-ttu-id="e0eef-169">Allgemeine Parameter</span><span class="sxs-lookup"><span data-stu-id="e0eef-169">Common parameters</span></span>

<span data-ttu-id="e0eef-170">PowerShell umfasst verschiedene *allgemeine Parameter*.</span><span class="sxs-lookup"><span data-stu-id="e0eef-170">PowerShell has several *common parameters*.</span></span> <span data-ttu-id="e0eef-171">Diese Parameter werden durch die PowerShell-Engine gesteuert.</span><span class="sxs-lookup"><span data-stu-id="e0eef-171">These parameters are controlled by the PowerShell engine.</span></span> <span data-ttu-id="e0eef-172">Allgemeine Parameter verhalten sich immer gleich.</span><span class="sxs-lookup"><span data-stu-id="e0eef-172">Common parameters always behave the same way.</span></span> <span data-ttu-id="e0eef-173">Die allgemeinen Parameter heißen **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable** und **OutBuffer**.</span><span class="sxs-lookup"><span data-stu-id="e0eef-173">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

### <a name="recommended-parameter-names"></a><span data-ttu-id="e0eef-174">Empfohlene Parameternamen</span><span class="sxs-lookup"><span data-stu-id="e0eef-174">Recommended parameter names</span></span>

<span data-ttu-id="e0eef-175">Der Kernsatz an PowerShell-Cmdlets verwendet standardmäßige Namen für ähnliche Parameter.</span><span class="sxs-lookup"><span data-stu-id="e0eef-175">The PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="e0eef-176">Die Verwendung von standardmäßigen Parameternamen wird nicht erzwungen, aber es gibt zur Förderung der Standardisierung explizite Anleitungen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-176">The use of these standard names is not enforced, but there is explicit guidance to encourage standardization.</span></span>

<span data-ttu-id="e0eef-177">Beispielsweise wird für einen Parameter, der anhand des Namens auf einen Computer verweist, die Bezeichnung **ComputerName** empfohlen, statt „Server“, „Host“, „System“, „Node“ oder eine andere gängige Alternative zu wählen.</span><span class="sxs-lookup"><span data-stu-id="e0eef-177">For example, the recommended name for a parameter that refers to a computer is **ComputerName**, rather than Server, Host, System, Node, or some other common alternative.</span></span> <span data-ttu-id="e0eef-178">Weitere empfohlene Parameternamen sind **Force**, **Exclude**, **Include**, **PassThru**, **Path** und **CaseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="e0eef-178">Other important recommended parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>
